# Workspace direction contract

Mode: Operate. Code-first, explicitly chosen by the user. Product and scope are the supplied brief. Routine design decisions are delegated.

Grounded candidates in resonance order: (1) code review's readable activity ledger, (2) experiment notebook with retained evidence, (3) debugger's execution inspector, (4) filesystem navigation and file actions, (5) developer command launcher, (6) executable notebook cells, (7) service operations workbench.

Seed 55ab8174 assigns candidate 5: the developer command launcher. The main workspace begins with one large command entry, with its result/history directly below. The persistent machine's identity and lifecycle sit above it. Files and the mapped service live in a compact adjacent column; the terminal becomes a full-width working surface when opened. Every action has a visible outcome and durable identity. On narrow screens, retain the same reading order without nested panes.

Use light warm paper for daytime local work; near-black type and terminal surface, muted olive accent for primary actions, discrete semantic colors paired with words. DM Sans for interface typography, JetBrains Mono exclusively for command/code/data. Strong spacing, fine rules and restrained radii. Avoid decorative cards and fabricated dashboards.

Challengers were judged against developer identification and task clarity. Cutting bench: declined; keep its persistent place marker as a real latest-action marker. Type specimen: competitive in clarity; keep decisive type hierarchy without giant decorative letters. Origami: declined; retain explicit prerequisite order. Cloud spectrum: declined; keep uncertainty labeled rather than inferred from color. Event catalog: declined; keep precise identity references in history. Telop: declined; keep high-legibility brief feedback without overlay spectacle. No borrowed illustration or material costume.

Signature interaction: submit a command and keep its submitted form anchored beside its durable outcome; a reconnect restores the same history. The terminal is an actual PTY, not a simulated animation.

Main risk: too many controls compete with first-run learning. Use one primary action per state and progressive disclosure for console diagnostics/recovery. The app remains usable without reading the whole walkthrough.

Verification: desktop and narrow browser screenshots, keyboard focus/escape, terminal resize, actual failure states. Final independent Impeccable review and documenter are required.
