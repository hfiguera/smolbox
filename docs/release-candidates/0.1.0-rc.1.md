# SmolBox 0.1.0-rc.1 validation

Status: candidate preparation in progress. No completed final-candidate matrix
or publication is claimed by this initial record.

The candidate's exact commit, immutable tag, package SHA-256 and validation
results will be recorded here after execution. This report is repository-only
material and is excluded from the Hex package. A later attestation commit may
update this report without changing the tested candidate or package bytes.

The accepted scope is the tested client/controller contract with development
workers. Production resource/isolation certification, protected real-worker
GitHub provisioning and independent consumer review are outside this release.
Unsupported hard controls and uncertain outcomes keep their existing behavior.

Required evidence: ordinary GitHub CI; canonical analyzers, canaries and coverage;
all six Linux/macOS Elixir/OTP compatibility lanes; real durable-store checks;
14 real client/runtime and 25 durable-recovery cases plus three service-fault
scenarios per platform; both host examples; four fresh package consumers of one
archive; version, changelog, license, source metadata and current Hex-name review.
